# BysterPlugin v3.9.3

Мощный плагин для Carbon/Rust серверов, добавляющий ботов целей с продвинутым движением, систему бесконечных патронов, систему брони и гибкую систему конфигурации.

## 📋 Команды

### Управление ботами

| Команда | Описание | Пример |
|---------|----------|---------|
| `/bspawn` | Заспавнить бота в точку прицела | `/bspawn` |
| `/bclear` | Удалить всех ботов | `/bclear` |
| `/bmove` | Переключить движение всех ботов | `/bmove` |
| `/remove` | Удалить объект в точке прицела | `/remove` |
| `/binfo` | Показать информацию о боте | `/binfo` |

### Настройки движения

| Команда | Описание | Пример |
|---------|----------|---------|
| `/bdelay [секунды]` | Установить задержку между сменами пути | `/bdelay 0.5` |
| `/bdegree` | Управление поворотами ботов | См. примеры ниже |

### Система брони ботов

| Команда | Описание | Пример |
|---------|----------|---------|
| `/barmor` | Показать текущие настройки брони | `/barmor` |
| `/barmor [тип]` | Установить тип брони для новых ботов | `/barmor hqm` |
| `/barmor auto [true/false]` | Включить/выключить автоэкипировку | `/barmor auto true` |
| `/barmor apply` | Применить броню ко всем ботам | `/barmor apply` |

### Система бесконечных патронов

| Команда | Описание | Пример |
|---------|----------|---------|
| `/infammo` | Переключить бесконечные патроны | `/infammo` |

### Система конфигурации

| Команда | Описание | Пример |
|---------|----------|---------|
| `/bconfig list` | Показать все настройки | `/bconfig list` |
| `/bconfig get [путь]` | Показать значение настройки | `/bconfig get MovementConfig.PathDelay` |
| `/bconfig set [путь] [значение]` | Установить значение | `/bconfig set SpawningConfig.MaxBotsAllowed 100` |
| `/bconfig reset [путь/all]` | Сбросить настройки | `/bconfig reset all` |

### Консольные команды

| Команда | Описание | Пример |
|---------|----------|---------|
| `byster.spawn` | Заспавнить бота в точку прицела | `byster.spawn` |
| `byster.clear` | Удалить всех ботов | `byster.clear` |
| `byster.move` | Переключить движение всех ботов | `byster.move` |
| `byster.info` | Показать информацию о боте | `byster.info` |
| `byster.remove` | Удалить объект в точке прицела | `byster.remove` |
| `byster.infammo` | Переключить бесконечные патроны | `byster.infammo` |
| `byster.config` | Управление конфигурацией | `byster.config list` |

## 🎯 Продвинутые примеры использования

### Система брони ботов (/barmor)

#### Доступные типы брони

Полный металлический сет (HQM)

```
/barmor hqm
```

Броня из дорожных знаков

```
/barmor signs
```

Хазмат костюм

```
/barmor hazmat
```

Без брони

```
/barmor naked
```

#### Управление автоэкипировкой

Включить автоматическую экипировку новых ботов

```
/barmor auto true
```

Отключить автоматическую экипировку

```
/barmor auto false
```

Применить текущий тип брони ко всем существующим ботам

```
/barmor apply
```

### Настройка поворотов ботов (/bdegree)

#### Базовые команды

Показать текущие настройки

```
/bdegree
```

Быстрая установка фиксированного угла (обратная совместимость)

```
/bdegree 180
```

#### Режимы поворота при спавне

Случайный поворот (0-360°)

```
/bdegree mode random
```

Фиксированный поворот

```
/bdegree mode fixed
/bdegree angle 90 # Все боты смотрят на восток
```

Поворот в диапазоне

```
/bdegree mode range
/bdegree angle 180 # Базовый угол - юг
/bdegree range 90 # Диапазон ±45° (юг-запад до юго-востока)
```

#### Настройка поворотов во время движения

Случайное направление движения

```
/bdegree movement random
```

Движение в фиксированном направлении (изначальный угол спавна)

```
/bdegree movement fixed
```

Движение всегда вперед (относительно текущего поворота)

```
/bdegree movement forward
```

Сохранять поворот при спавне (боты не поворачиваются во время движения)

```
/bdegree keep true
```

#### Практические сценарии

Патрульные боты: спавн в случайном направлении, движение вперед

```
/bdegree mode random
/bdegree movement forward
/bdegree keep false
```

Стражи: все смотрят на вход (север), не поворачиваются

```
/bdegree mode fixed
/bdegree angle 0
/bdegree keep true
```

Хаотичные боты: все случайно

```
/bdegree mode random
/bdegree movement random
/bdegree keep false
```

### Система конфигурации (/bconfig)

#### Настройка лимитов и производительности

Увеличить лимит ботов

```
/bconfig set SpawningConfig.MaxBotsAllowed 100
```

Отключить автореспавн

```
/bconfig set SpawningConfig.AutoRespawn false
```

Ускорить движение ботов

```
/bconfig set MovementConfig.PathDelay 0.05
/bconfig set MovementConfig.MovementUpdateRate 0.02
```

Увеличить дистанцию движения

```
/bconfig set MovementConfig.MovementDistance 25.0
```

#### Настройка брони ботов

Изменить тип брони по умолчанию

```
/bconfig set ArmorConfig.CurrentArmorType hqm
```

Отключить автоэкипировку новых ботов

```
/bconfig set ArmorConfig.AutoEquipNewBots false
```

#### Отладка и логирование

Включить подробное логирование

```
/bconfig set SystemConfig.LogDebugInfo true
```

Изменить цвет сообщений

```
/bconfig set SystemConfig.ChatColor "#FF0000"
```

Отключить бесконечные патроны по умолчанию

```
/bconfig set SystemConfig.AllowInfiniteAmmo false
```

#### Сброс настроек

Сбросить конкретную настройку

```
/bconfig reset MovementConfig.PathDelay
```

Сбросить всю категорию настроек

```
/bconfig reset MovementConfig
```

Полный сброс всех настроек

```
/bconfig reset all
```

### Консольные команды

#### Управление ботами через консоль

Заспавнить бота (эквивалент `/bspawn`)

```
byster.spawn
```

Удалить всех ботов (эквивалент `/bclear`)

```
byster.clear
```

Переключить движение ботов (эквивалент `/bmove`)

```
byster.move
```

#### Управление конфигурацией через консоль

Показать все настройки

```
byster.config list
```

Получить значение настройки

```
byster.config get MovementConfig.PathDelay
```

Установить значение настройки

```
byster.config set SpawningConfig.MaxBotsAllowed 100
```

#### Преимущества консольных команд

- Можно использовать в скриптах автоматизации
- Доступны через RCON
- Более подробный вывод в консоль сервера
- Можно использовать в планировщике задач

## ⚙️ Технические особенности

### Система движения
- **Обход препятствий**: Боты автоматически обходят стены и объекты
- **Привязка к поверхности**: Автоматическое определение земли и препятствий
- **Проверка высоты**: Боты не могут подниматься выше установленного лимита
- **Плавное движение**: 20 FPS обновление позиций для реалистичности

### Система поворотов
- **Три режима спавна**: fixed (фиксированный), random (случайный), range (диапазон)
- **Три режима движения**: fixed (в направлении спавна), random (случайно), forward (вперед)
- **Сохранение ориентации**: Возможность запретить поворот во время движения

### Система брони
- **Четыре типа экипировки**: HQM (металлическая), Signs (дорожные знаки), Hazmat (хазмат), Naked (без брони)
- **Автоэкипировка**: Возможность автоматически экипировать ботов при спавне
- **Ручное применение**: Команда для применения брони ко всем существующим ботам

### Автореспавн
- **Сохранение позиции**: Боты респавнятся на месте смерти
- **Управление трупами**: Автоматическое удаление тел ботов
- **Настраиваемые задержки**: От мгновенного до длительного респавна

## 📊 Мониторинг и отладка

### Команда /binfo
Показывает подробную информацию о боте:
- Позиция и углы обзора
- Состояние движения
- Флаги и статус корутин
- Проверка активности

### Логирование
При включении `LogDebugInfo` в конфигурации, плагин выводит подробную информацию о:
- Спавне и удалении ботов
- Изменении настроек движения
- Работе системы автореспавна

## 🛠️ Устранение неполадок

### Боты не двигаются
1. Проверьте: `/bmove` (возможно движение отключено)
2. Проверьте: `/binfo` (статус корутин)
3. Убедитесь что боты живы и не ранены

### Боты застревают
1. Уменьшите `MovementConfig.StepSize`
2. Увеличьте `MovementConfig.MaxClimbHeight`
3. Проверьте окружающую местность на препятствия

### Проблемы с производительностью
1. Уменьшите `SpawningConfig.MaxBotsAllowed`
2. Увеличьте `MovementConfig.MovementUpdateRate`
3. Увеличьте `MovementConfig.PathDelay`

### Проблемы с экипировкой
1. Проверьте корректность названий предметов
2. Убедитесь, что автоэкипировка включена
3. Используйте команду `/barmor apply` для принудительного применения

## 🖥️ Администрирование через консоль

### Удаленное управление через RCON

Плагин поддерживает все команды через консоль сервера, что позволяет управлять им через RCON:

```
rcon byster.config list
rcon byster.spawn
rcon byster.clear
```

### Автоматизация с помощью скриптов

Пример скрипта для автоматического перезапуска ботов каждый час:

```bash
#!/bin/bash
while true; do
  echo "Перезапуск ботов..."
  rcon byster.clear
  sleep 5
  rcon byster.spawn
  rcon byster.spawn
  rcon byster.spawn
  echo "Боты перезапущены"
  sleep 3600
done
```

### Планирование задач

Можно использовать планировщик задач сервера для выполнения команд по расписанию:

```
# Очистка ботов каждую полночь
0 0 * * * rcon byster.clear

# Спавн новых ботов через 5 минут после очистки
5 0 * * * rcon byster.spawn
```

### Логирование действий

При включенном `LogDebugInfo` все консольные команды записываются в лог сервера, что позволяет отслеживать кто и когда использовал команды плагина.

---

Для подробной информации о всех настройках конфигурации см. [config.md](config.md)